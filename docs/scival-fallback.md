# SciVal institution-count fallback

The standard SciVal publication export is used only when neither WoS Expanded nor Scopus provides a usable institution count. Matching priority inside SciVal is Scopus EID/ID, DOI, then an unambiguous exact normalized title + year fallback. SciVal does not by itself establish Ministry eligibility.
